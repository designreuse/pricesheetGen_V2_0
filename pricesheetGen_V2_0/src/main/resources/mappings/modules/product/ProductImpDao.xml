<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sapling.modules.product.dao.ProductImpDao">
	
	<sql id="productImpColumns">
		a.id as id,
		a.pId as pId,
		a.pName as pName,
		a.bName as brand,
		a.cName as classfiy,
		a.mName as model,
		a.delStatus as delStatus,
		a.create_by as  "createBy.id",
		b.`name` as "createBy.name",
		a.update_by as "updateBy.id",
		c.`name` as "updateBy.name",
		a.create_date as createDate,
		a.update_date as updateDate
	</sql>

	<select id="get" resultType="ProductImp">
		SELECT 
		    DISTINCT
			<include refid="productImpColumns"/>
		FROM t_product_imp   a  
		LEFT JOIN sys_user b on a.create_by=b.id
		LEFT JOIN sys_user c on a.update_by=c.id	
		WHERE a.id = #{id}
	</select>
	
	<select id="findPageList" resultType="ProductImp" >
		SELECT 
		DISTINCT
			<include refid="productImpColumns"/>
		FROM t_product_imp   a  
		LEFT JOIN sys_user b on a.create_by=b.id
		LEFT JOIN sys_user c on a.update_by=c.id 			
		<where>
			1=1
		     <if test="pName !=null and pName !='' ">
				and a.pName LIKE CONCAT('%',#{pName},'%' ) 
			</if>
			and delStatus=0
		</where>
		ORDER  BY a.update_date
	</select>

	<insert id="insert">	
		INSERT INTO t_product_imp(
			id,
			pId,
			pName,
			bName,
			cName,
			mName,
			delStatus,
			create_date,
			create_by,
			update_date,
			update_by
		) VALUES (
			#{id},
			#{pId},
			#{pName},
			#{brand},
			#{classfiy},
			#{model},
			#{delStatus},
			#{createDate},
			#{createBy.id},
			#{updateDate},
			#{updateBy.id}
		)		
	</insert>
 
	<!--逻辑删除-->
	<update id="delete">
		UPDATE t_product_imp SET 
			delStatus = 1
		WHERE pName = #{pName}
	</update>
	 
</mapper>